{
  "name": "protoform-monorepo",
  "private": true,
  "scripts": {
    "build": "bun run --filter '@protoform/core' build && bun run --filter '@protoform/react' build && bun run --filter '@protoform/adapter-*' build && bun run --filter 'protoform-demo' build",
    "build:core": "bun run --filter '@protoform/core' build",
    "build:react": "bun run --filter '@protoform/react' build",
    "dev": "bun run --filter 'protoform-demo' dev",
    "type-check": "bun run --filter '*' type-check",
    "test": "vitest run packages/core/tests/ packages/react/tests/",
    "conformance": "bun conformance/validate-schemas.mjs && bun conformance/run-ts.mjs > conformance/.out-ts.json && python3 conformance/run-py.py > conformance/.out-py.json && bun conformance/diff.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "packageManager": "bun@1.3.9",
  "trustedDependencies": [
    "esbuild"
  ]
}
